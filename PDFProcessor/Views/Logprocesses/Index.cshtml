@model IEnumerable<PDFProcessor.Models.Logprocess>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.OriginalFileName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExistingState)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NewFileName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateProcces)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.OriginalFileName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExistingState)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NewFileName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateProcces)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
